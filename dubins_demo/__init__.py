"""Dubins Path Demonstrator.

Desktop app (Tkinter + matplotlib + numpy) that computes and demonstrates
Dubins paths. See docs/specs and docs/design for the full specification.

This package is currently scaffolding only — the core math layer, UI, and
persistence modules land in subsequent work. See the design doc's package
layout (docs/design/2026-07-17-dubins-demo-design.md, section 1).
"""

__version__ = "0.1.0"
